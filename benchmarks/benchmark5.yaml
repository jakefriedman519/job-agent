# Easy Benchmark Problem
# Test case: Find jobs by skill
# This test case checks if the agent can successfully return a list of job listings based on a skill.
# The agent retrieves all jobs mentioning "Python" in the skills or job description.

- prompt: 'List jobs that require Python.'
  expected_type: 'find-jobs'
  expected_keywords: ['python']
